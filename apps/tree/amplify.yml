version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd apps/tree
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: apps/tree/build
    files:
      - '**/*'
  cache:
    paths:
      - apps/tree/node_modules/**/* 